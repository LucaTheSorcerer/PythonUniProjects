"""
    str.upper(): Returns a copy of the string with all the characters in uppercase.
    str.lower(): Returns a copy of the string with all the characters in lowercase.
    str.capitalize(): Returns a copy of the string with the first character in uppercase and the rest in lowercase.
    str.title(): Returns a copy of the string with the first letter of each word in uppercase and the rest in lowercase.
    str.swapcase(): Returns a copy of the string with all the uppercase characters in lowercase and vice versa.
    str.strip(): Returns a copy of the string with leading and trailing whitespaces removed.
    str.lstrip(): Returns a copy of the string with leading whitespaces removed.
    str.rstrip(): Returns a copy of the string with trailing whitespaces removed.
    str.replace(old, new): Returns a copy of the string with all occurrences of the old substring replaced by the new substring.
    str.split(sep): Returns a list of substrings obtained by splitting the string at each occurrence of the separator (sep).
    str.join(iterable): Returns a string in which the elements of the iterable (such as a list) have been joined by the string on which the method is called.
    str.find(sub): Returns the lowest index of the substring (sub) in the string, or -1 if the substring is not present.
    str.rfind(sub): Returns the highest index of the substring (sub) in the string, or -1 if the substring is not present.
    str.index(sub): Like find(), but raises a ValueError if the substring is not present.
    str.rindex(sub): Like rfind(), but raises a ValueError if the substring is not present.
    str.count(sub): Returns the number of non-overlapping occurrences of the substring (sub) in the string.
    str.startswith(prefix): Returns True if the string starts with the specified prefix, False otherwise.
    str.endswith(suffix): Returns True if the string ends with the specified suffix, False otherwise.
    str.isalnum(): Returns True if all the characters in the string are alphanumeric (i.e., alphabets and/or digits), False otherwise.
    str.isalpha(): Returns True if all the characters in the string are alphabets, False otherwise.
    str.isdigit(): Returns True if all the characters in the string are digits, False otherwise.
    str.islower(): Returns True if all the characters in the string are in lowercase, False otherwise.
    str.isupper(): Returns True if all the characters in the string are in uppercase, False otherwise.
    str.isspace(): Returns `
    
"""